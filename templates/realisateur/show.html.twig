{% extends 'admin/layout.html.twig' %}

{% block title %}Realisateur{% endblock %}

{% block body %}
    <h1>Realisateur</h1>
{# {{dump()}} #}
    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ realisateur.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ realisateur.nom }}</td>
            </tr>
            <tr>
                <th>Portrait</th>
                <td>{{ realisateur.portrait }}</td>
            </tr>
            <tr>
                <th>Film</th>
                  {% for realisateur in realisateur.films %}
                  
                <td><ul><li>{{ realisateur.titre }}</li></ul></td>
                {% endfor %}
            </tr>
        </tbody>
    </table>

    <a href="{{ path('realisateur_index') }}">back to list</a>

    <a href="{{ path('realisateur_edit', {'id': realisateur.id}) }}">edit</a>

    {{ include('realisateur/_delete_form.html.twig') }}
{% endblock %}
